name: changes_info
on: [pull_request]

jobs:
  compile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: '3.x'
      - run: sudo apt-get install libyaml-dev
      - name: install python dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
      - id: files
        uses: jitterbit/get-changed-files@v1
      - run: |
          for changed_file in ${{ steps.files.outputs.added_modified }}; do
            file_count=$(find pokesets/ -name "$changed_file" | wc -l)
            if [[ $(find pokesets/ -name "$changed_file" | wc -l) -gt 0 && ( $changed_file == *.yaml || $changed_file == *.yml ) ]]; then
              echo "$changed_file is a pokeset"
              python -m pokecat populate "${changed_file}" "/tmp/populated_${changed_file}"
              python -m pokecat instantiate "/tmp/populated_${changed_file}" "/tmp/instantiated_${changed_file}"
              comment=$(cat "/tmp/instantiated_${changed_file}" | jq --raw-output '.[] | "\(.setname): \(.stats)"')
              echo "test comment: ${comment}."
            else
              echo "$changed_file is not a pokeset"
            fi
          done
